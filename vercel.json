{
  "buildCommand": "npx esbuild src/entry.ts --bundle --platform=node --target=node20 --outfile=api/index.mjs --external:argon2 --external:mongoose --format=esm",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
