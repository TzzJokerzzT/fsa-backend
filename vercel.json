{
  "buildCommand": "npx esbuild src/entry.ts --bundle --platform=node --target=node20 --outfile=api/index.js --format=cjs",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
